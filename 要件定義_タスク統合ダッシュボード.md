# 要件定義・詳細設計 1枚

**プロダクト名（仮）**: OneDesk / TodayBoard など後で決定
**チーム**: 2名
**お題の切り口**: B. 学生生活の無駄（＋C. チーム開発の無駄の要素）

---

## 1. コンセプト（誰の・どんな無駄を・なぜ）

> **複数のツールを日常的に使う学生・社会人**は、朝「今日やること」を把握するために **Gmail・Slack・Googleカレンダーを順番に開いて回る**必要がある。**情報が3か所に分散していて全体像が一度に見えない**ため、確認そのものに時間がかかり、さらに見落としが発生する。**すべてのタスクを1つのリストに正規化して表示する**ことで解決する。

### 「見える化」の先にある行動変化（評価軸25点はここ）

- 朝の巡回（3アプリ × 各2〜3分）が **1画面30秒**になる
- 期限切れ・今日締切が**赤で最上部に固定**されるので、埋もれた依頼を拾える
- 「見えるようになった」ではなく「**朝の確認行動が1回で終わる**」と言い切る

---

## 2. スコープ

### Must（これだけで完走できる設計）

| # | 機能 | 完了条件 |
|---|---|---|
| M1 | タスク統合ビュー | 全ソースのタスクを「期限切れ / 今日 / 今週 / 期限なし」に分類して1画面表示 |
| M2 | 外部ソース連携（**1つだけ**） | Googleカレンダーの予定を取得し、tasksテーブルに正規化して保存 |
| M3 | タスクの手動追加・状態更新 | フォームから1件追加できる／ワンクリックで完了にできる |

### Should（時間が余ったら）

- Gmail連携（**スター付き**または特定ラベルのメールのみに限定）
- 優先度の手動変更、並び替え
- 同期ボタンの自動実行（定期ポーリング）

### Could（やらない。スライドの「今後の展望」に書く）

- Slack連携（管理者承認リスクのため当日は着手しない）
- リアルタイム同期、Webhook購読
- 通知・プッシュ、チーム共有機能、AI要約

---

## 3. 段階設計：デモが絶対に失敗しない構造

```
[ソース層]              [正規化層]           [表示層]
Googleカレンダー ──┐
Gmail（Should）  ──┼──→ tasks テーブル ──→ ダッシュボードUI
手動入力（Must）  ──┘    （共通スキーマ）
```

**重要**: 表示層は tasks テーブルだけを見る。ソースが1つでもゼロでも画面は成立する。
連携が当日壊れても、手動追加でデモデータを入れれば発表できる。この構造自体が「設計の工夫」としてスライドに書ける。

---

## 4. DBスキーマ（Supabase / PostgreSQL）

認証は Supabase Auth（`auth.users`）をそのまま利用。自前実装しない。

### connections（外部サービス連携情報）

| カラム | 型 | 備考 |
|---|---|---|
| id | uuid PK | |
| user_id | uuid FK → auth.users | |
| provider | text | `google_calendar` / `gmail` / `slack` |
| access_token | text | 暗号化して保存 |
| refresh_token | text | |
| expires_at | timestamptz | |
| created_at | timestamptz | default now() |

### tasks（正規化タスク）

| カラム | 型 | 備考 |
|---|---|---|
| id | uuid PK | |
| user_id | uuid FK → auth.users | |
| source | text | `google_calendar` / `gmail` / `manual` |
| external_id | text | 外部側のID。manualはnull |
| title | text | NOT NULL |
| url | text | 元データへのリンク |
| due_at | timestamptz | null許容（期限なしタスク） |
| status | text | `todo` / `done` / `ignored`、default `todo` |
| priority | int | default 0 |
| raw | jsonb | 取得した生データ（デバッグ用） |
| synced_at | timestamptz | |
| created_at | timestamptz | default now() |

**制約**: `UNIQUE (user_id, source, external_id)`
→ 再同期時は upsert で重複を防ぐ。ここを忘れると同期のたびに同じ予定が増殖する。

**RLS**: 両テーブルとも `auth.uid() = user_id` のポリシーを設定。5限のRDB知識＋RLSがそのまま活きる部分なので、スライドの技術選定理由に書く。

---

## 5. 画面遷移（3枚）

```
/login  ──ログイン──→  /  ──設定ボタン──→  /settings
（Supabase Auth）      （ダッシュボード）    （連携・同期）
                            ↑                    │
                            └────戻る─────────────┘
```

### `/` ダッシュボード（メイン画面）

- 上から順に: **期限切れ（赤） / 今日（強調） / 今週 / 期限なし**
- 各タスク行: ソースアイコン・タイトル・期限・完了チェックボックス・元リンク
- 右上に「＋タスク追加」ボタン（モーダル）
- ヘッダーに件数サマリ（例: 期限切れ2 / 今日5）

### `/settings`

- 「Googleカレンダーと連携する」ボタン（OAuth開始）
- 連携済みなら「今すぐ同期」ボタン＋最終同期時刻

---

## 6. API一覧（Next.js Route Handlers）

| メソッド | パス | 役割 |
|---|---|---|
| GET | `/api/tasks` | タスク一覧取得（status絞り込み） |
| POST | `/api/tasks` | 手動タスク追加 |
| PATCH | `/api/tasks/:id` | status・priority更新 |
| GET | `/api/auth/google` | OAuth開始（リダイレクト） |
| GET | `/api/auth/google/callback` | トークン受領・connections保存 |
| POST | `/api/sync/google` | カレンダー取得→tasksへupsert |

計6本。これ以上増やさない。

---

## 7. 技術スタック（要項5章準拠）

| レイヤー | 技術 | 選定理由（スライドにそのまま使える） |
|---|---|---|
| フロント | Next.js (App Router) / TypeScript | 情報が豊富、Cloudflareとの相性 |
| DB・認証 | Supabase (PostgreSQL) | RLSで権限制御が自前実装不要。OAuthトークン保管も一元化 |
| インフラ | Cloudflare Workers / Pages | 規約必須。無料枠・デプロイ高速 |
| バージョン管理 | Git / GitHub（PRベース） | 7限・8限の実践 |
| AI | Claude Code | 10限の実践 |

### ⚠ 技術リスク

**Next.js を Cloudflare にデプロイする部分が最大の詰まりどころ**です（OpenNext / next-on-pages の設定）。
実装③のデプロイ工程で30分以上溶けたら、**Vite + React（静的） + Hono on Workers** に切り替える判断をあらかじめ決めておく。この切り替え基準を先に決めておくこと自体がスコープ管理の工夫としてスライドに書けます。

---

## 8. 当日タイムライン（要項4章に対応）

| 時間 | やること | 担当 |
|---|---|---|
| 0:00–0:05 | オリエン・役割確認 | 両名 |
| 0:05–0:35 | 本ドキュメントをチームでレビュー・確定 | 両名 |
| 0:35–1:35 | リポジトリ作成、CLAUDE.md、Supabase接続、認証、テーブル作成 | A: 認証 / B: DB・UI土台 |
| 1:35–1:40 | 中間チェック① | |
| 1:40–3:35 | M1・M2・M3の実装 | A: OAuth＋同期API / B: ダッシュボードUI＋CRUD |
| 3:35–3:40 | 中間チェック②（Must完成判定） | |
| 3:40–4:35 | Cloudflareデプロイ、デモ動画、スライド | A: デプロイ / B: スライド |
| 4:35–5:20 | 発表・総括 | 両名 |

---

## 9. 事前準備チェックリスト（**当日までに必ず**）

- [ ] Google Cloud プロジェクト作成
- [ ] OAuth同意画面を「外部・テストモード」で作成、**テストユーザーにメンバー2名のメールを登録**
- [ ] スコープ追加: `calendar.events.readonly`（GmailをやるならScope追加時に再確認）
- [ ] クライアントID / シークレット発行
- [ ] リダイレクトURIを `http://localhost:3000/api/auth/google/callback` と本番URLの両方登録
- [ ] Supabaseプロジェクト作成、接続情報を控える
- [ ] Cloudflareアカウント作成、GitHub連携を確認
- [ ] Hello AI! organization への権限確認（publicリポジトリ作成可能か）

> テストモードなら審査なしで100ユーザーまで使えます。本番審査は不要です。

---

## 10. 役割分担（2名）

| | メンバーA | メンバーB |
|---|---|---|
| 主担当 | 認証・OAuth・同期API | DBスキーマ・ダッシュボードUI・タスクCRUD |
| ブランチ | `feature/auth-sync` | `feature/dashboard` |
| 共通 | CLAUDE.md整備、相互PRレビュー（承認1名必須）、GitHub Actions | |

**衝突回避**: 最初の30分でtasksテーブルの型定義（`types/task.ts`）だけ2人で確定させ、mainにマージしてから分岐する。これをやらないと結合時に必ず揉めます。

---

## 11. スライド必須項目とのマッピング（要項8章）

| 必須項目 | 本ドキュメントの参照先 |
|---|---|
| 技術スタック | 7章 |
| なぜそれを選んだのか | 7章の選定理由列 |
| Gitの活用内容・工夫点 | 10章＋当日のPR運用実績 |
| Agent活用の工夫点 | CLAUDE.md、Plan Mode、並列作業の記録 |
| コンセプト・作った理由 | 1章の1文をそのまま掲載 |
